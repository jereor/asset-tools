metadata:
  name: "base-config"
  description: " Base configuration for asset tools"
  version: 1.0

validation:
  failOnWarning: false

paths:
  assetsRoot: "tests/assets/"

logging:
  verbose: false
